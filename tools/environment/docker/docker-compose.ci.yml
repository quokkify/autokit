services:
  nginx:
    ports: []
  kafka:
    ports: []
  zookeeper:
    ports: []
  mock-server:
    ports: []
  centrifugo:
    ports: []
  mongodb:
    ports: []

networks:
  default:
    internal: true
